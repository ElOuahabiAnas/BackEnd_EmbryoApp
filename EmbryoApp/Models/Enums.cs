namespace EmbryoApp.Models;

public enum ModelStatus { Draft, Active, Closed }          // Status IN ('draft','active','Closed')
public enum MediaType   { Photo, Video }                   // MediaType IN ('photo','video')
